name: 'adventure_works'
version: '1.0.0'
config-version: 2

profile: 'default'

model-paths: ["models"]
test-paths: ["tests"]

models:
  adventure_works:
    staging:
      +materialized: view
    marts:
      +materialized: table